import{logger}from"./domule/core.log.min.mjs";export const NAME="aos";const DEFAULTS={selector:"[data-aos]",dataName:"visible",offset:120,once:false,disableMutationObserver:false,threshold:.1};const THRESHOLD_STEPS=101;const ELEMENT_NODE=1;const RESIZE_DEBOUNCE_MS=200;const thresholdCache=new WeakMap;function parseThreshold(value){return typeof value==="string"&&value.endsWith("%")?parseFloat(value)/100:parseFloat(value)}function getElementThreshold(el,defaultThreshold){if(!thresholdCache.has(el)){const value=
el.dataset.aosThreshold??defaultThreshold;thresholdCache.set(el,parseThreshold(value))}return thresholdCache.get(el)}function initElement(el,options){el.classList.add("nok-aos");if(el.dataset[options.dataName]!=="true")el.dataset[options.dataName]="false";if(!el.dataset.aosOnce)el.dataset.aosOnce=String(options.once)}function isVisible(visibleHeight,elementHeight,viewportHeight,threshold){if(viewportHeight<=0)return false;if(elementHeight>0&&visibleHeight>=elementHeight*.99)return true;return visibleHeight/
viewportHeight>=threshold}class AOS{constructor(options={}){this.options={...DEFAULTS,...options};this.elements=new Set;this.observer=null;this.mutationObserver=null;this.initialized=false;this._onResize=null;this._onPageShow=null}init(){if(this.initialized){logger.warn(NAME,"Already initialized");return}const els=document.querySelectorAll(this.options.selector);if(!els.length){logger.info(NAME,"No elements found");return}els.forEach(el=>{initElement(el,this.options);this.elements.add(el)});this._setupIntersectionObserver();
this._checkInitialVisibility();if(!this.options.disableMutationObserver)this._setupMutationObserver();this._bindWindowEvents();this.initialized=true;logger.info(NAME,`Tracking ${this.elements.size} element(s)`)}_setupIntersectionObserver(){const rootMargin=`${this.options.offset}px 0px ${this.options.offset}px 0px`;this.observer=new IntersectionObserver(entries=>{entries.forEach(entry=>{const el=entry.target;const threshold=getElementThreshold(el,this.options.threshold);const viewportHeight=entry.rootBounds?.height??
window.innerHeight;const visibleHeight=entry.intersectionRect.height;const elementHeight=entry.boundingClientRect.height;if(isVisible(visibleHeight,elementHeight,viewportHeight,threshold)){el.dataset[this.options.dataName]="true";if(el.dataset.aosOnce==="true")this.observer.unobserve(el)}else if(el.dataset.aosOnce!=="true")el.dataset[this.options.dataName]="false"})},{rootMargin,threshold:Array.from({length:THRESHOLD_STEPS},(_,i)=>i/(THRESHOLD_STEPS-1))});this.elements.forEach(el=>{if(el.dataset.aosOnce===
"true"&&el.dataset[this.options.dataName]==="true")return;this.observer.observe(el)})}_checkInitialVisibility(){const viewportHeight=window.innerHeight+this.options.offset*2;this.elements.forEach(el=>{if(el.dataset[this.options.dataName]==="true")return;const rect=el.getBoundingClientRect();if(rect.bottom<-this.options.offset||rect.top>window.innerHeight+this.options.offset)return;const visibleTop=Math.max(rect.top,-this.options.offset);const visibleBottom=Math.min(rect.bottom,window.innerHeight+
this.options.offset);const visibleHeight=Math.max(0,visibleBottom-visibleTop);const threshold=getElementThreshold(el,this.options.threshold);if(isVisible(visibleHeight,rect.height,viewportHeight,threshold)){el.dataset[this.options.dataName]="true";if(el.dataset.aosOnce==="true")this.observer?.unobserve(el)}})}_setupMutationObserver(){this.mutationObserver=new MutationObserver(mutations=>{mutations.forEach(mutation=>{mutation.addedNodes.forEach(node=>{if(node.nodeType!==ELEMENT_NODE)return;if(node.matches(this.options.selector))this._addElement(node);
node.querySelectorAll?.(this.options.selector).forEach(el=>this._addElement(el))})})});this.mutationObserver.observe(document.body,{childList:true,subtree:true})}_bindWindowEvents(){let resizeTimer;this._onResize=()=>{clearTimeout(resizeTimer);resizeTimer=setTimeout(()=>this.refresh(),RESIZE_DEBOUNCE_MS)};window.addEventListener("resize",this._onResize);this._onPageShow=e=>{if(e.persisted){logger.info(NAME,"bfcache restore \u2014 re-checking visibility");this._checkInitialVisibility()}};window.addEventListener("pageshow",
this._onPageShow)}_unbindWindowEvents(){if(this._onResize){window.removeEventListener("resize",this._onResize);this._onResize=null}if(this._onPageShow){window.removeEventListener("pageshow",this._onPageShow);this._onPageShow=null}}_addElement(el){if(this.elements.has(el))return;initElement(el,this.options);this.elements.add(el);this.observer?.observe(el);logger.info(NAME,"Added dynamic element")}refresh(){this.observer?.disconnect();document.querySelectorAll(this.options.selector).forEach(el=>{if(!this.elements.has(el)){initElement(el,
this.options);this.elements.add(el)}});this._setupIntersectionObserver();this._checkInitialVisibility();logger.info(NAME,"Refreshed")}refreshHard(){this.destroy();this.init()}destroy(){this.observer?.disconnect();this.mutationObserver?.disconnect();this._unbindWindowEvents();this.elements.forEach(el=>{delete el.dataset[this.options.dataName];thresholdCache.delete(el)});this.elements=new Set;this.observer=null;this.mutationObserver=null;this.initialized=false;logger.info(NAME,"Destroyed")}}export default{init(options=
{}){const aos=new AOS(options);if(document.readyState==="loading")document.addEventListener("DOMContentLoaded",()=>aos.init());else aos.init();return aos}};
